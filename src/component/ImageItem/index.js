import React,{useEffect,useState} from 'react';import { useNavigate } from 'react-router-dom'import classnames from 'classnames';import { inject,observer } from 'mobx-react'import * as urls from '@/constant/urls'import {API_SERVER} from '@/constant/apis'import s from './index.module.less';import right from '@/img/right.svg'const ImageItem = ({item, bottom=false, big=false}) => {  const navigate = useNavigate();      const doToProd=(e)=>{    navigate(`/prod?id=${e.id}&sub=${e.sub}&name=${e.name}`)  }   return (    <div className={classnames(s.imgItem,{big:big})}>      <img src={item.img} onClick={()=>doToProd(item)}/>      <span className={s.spec}>{item.spec}</span>      {bottom &&      <div className={s.wrap}>        <img src={right} />        <span>{item.sub}</span>      </div>}    </div>  )      }export default  ImageItem